import 'package:flutter/material.dart';

class EcommerceMoreScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Ecommerce Test Screen"),
      ),
    );
  }
}
